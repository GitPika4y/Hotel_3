# Hotel

# Требования проекта

Данный проект был разработан на:

IDE

- JetBrains Rider 2025.2.3
- .NET 8.0
- MSSQLServer

с использованием пакетов NuGet

- EntityFrameworkCore / 9.0.9
- EntityFrameworkCore.Tools / 9.0.9
- EntityFrameworkCore.SqlServer / 9.0.9
- EntityFrameworkCore.Proxies / 9.0.0 
- Extensions.Configuration / 10.0.0
- Extensions.DependencyInjection / 9.0.9
- CommunityToolkit.Mvvm / 8.4.0
- MaterialDesignthemes / 5.3.1
- Newtonsoft.Json / 13.0.4
- DotNetEnv / 3.1.1

# Описание проекта

Данный учебный проект был выполнен по ТЗ `КОД 09.02.07-5-2025 Приложения к образцу задания Том 1`
(отсутствует в репозиторие).

Решение (репозиторий) разделено на 3 проекта:

- Hotel_3.Domain
- Hotel_3.EntityFramework
- Hotel_3.WPF

***Несмотря на то что проект был разработан в JetBrains Rider, по большей части, инструкция по запуску будет ссылаться
на Visual Studio 2022***

# 🚀 Запуск проекта

## 1. Склонируйте репозиторий:

Для этого выполните команду

```bash
git clone https://github.com/GitPika4y/Hotel_3
```

## 2. Укажите строку подключения

В представленном файле `.env` представлена строка для подключения к локальному серверу MSSQLServer,
с входом по аутентификации Windows (т.е. без пользователя и пароля)

Чтобы изменить строку подключения, укажите собственное значение переменной окружения DB_CONNECTION_STRING,
например: 

```
DB_CONNECTION_STRING=Server=someserver;Database=My Database;User Id=some user;Password=password;Trusted_Connection=True;TrustServerCertificate=True;
```

заменяя значения `someserver`, `some user`, `My Database`, `password` на свои.

Убедитесь, что указанная база данных существует на сервере и что подключение к серверу доступно.

При отсутствии таковой БД, на сервере появится новая, с тем именем, что было указано в строке подключения.
(при указании строки подключения из примера)

[Если произошла ошибка](#ошибка-строки-подключения)

## 3. Примените миграции

Если у вас `Visual Studio`, то выберите: `Средства -> Диспетчер пакетов NuGet -> Консоль диспетчера пакетов`.
Должна будет появится консоль с припиской `PM>`. 

В данной консоли выберите `Проект по умолчанию`: `Hotel_3.EntityFramework`

В самом `Visual Studio` также выберите `Hotel_3.EntityFramework` как `Проект по умолчанию`

После чего выполните данную команду

```bash
update-database
```

[Если произошла ошибка](#ошибка-миграции)

Или если у вас `JetBrains Rider`, кликните правой кнопкой мыши на проект `Hotel_3.EntityFramework`,
и выберите `Entity Framework Core > Update-Database`

После успешно примененных миграций, в БД появятся новые таблицы:

`__EFMigrationsHistory`, `Bookings`, `Clients`, `Roles`, `RoomCategories`, `Rooms`, `RoomStatuses`, `Users`

Что будет означать что миграция выполнена успешно

## 4. Добавьте админа

Данный проект предполагает наличие хотя бы 1 пользователя (админа).
Приложение ***не создает админа автоматически***, поэтому перед первым запуском нужно создать пользователя вручную.

Для этого зайдите в MSSQLServer и выполните следующее в вашей БД:

### Вариант 1
Зайдите в таблицу `Users`, и добавьте новую запись, также пропуская Id,
и вставив в поле `RoleId` `Id` ранее добавленной роли.

- ### Создайте роль

Для добавления роли, зайдите в созданную ранее таблицу `Roles`, и добавьте новую запись 
с пропуском `Id` (после создания сущности он вставится автоматически), и указав в `Name` значение `Админ`

Создав роль, запомните её `Id` (если всё получится с первого раза, то он будет равен `1`)

- ### Создайте пользователя

Для этого, зайдите в таблицу `Users`, и добавьте новую запись, также пропустив `Id`,
указав в `Login` и `Password` собственные значения, а в `RoleId` - `1`
(или другой, смотря под каким `Id` у вас находится запись `Админ` в таблице `Roles`)

### Вариант 2
Впишите следующую команду в Sql запрос,
при этом заменив `admin` и `password` на собственные значения:

```bash
INSERT INTO Roles (Name) VALUES ('Админ');
INSERT INTO Users (Login, Password, RoleId) VALUES ('admin', 'password', 1);
```

Убедитесь что строки были добавлены в соответствующие таблицы

## 5. Запустите приложение

Откройте решение проекта с помощью файла Hotel_3.sln.

Если вы уже видите три проекта
`Hotel_3.Domain`, `Hotel_3.EntityFramework`, `Hotel_3.WPF`
— значит решение уже открыто, и дополнительное действие не требуется.

Далее необходимо сделать запускаемым проектом Hotel_3.WPF.

### Visual Studio

1. В верхней панели нажмите на выпадающий список рядом с кнопкой запуска.
2. Выберите "Настройка начальных проектов…"

3. Укажите:  
Один запускаемый проект → Hotel_3.WPF → OK

Убедитесь, что приложение сможет подключиться к вашему SQL Server.

Теперь проект готов к запуску.

# Работа с данными

В данном приложении существует функционал `Экспорта` и `Импорта` данных (в виде JSON файла).

## Тестовые данные

В репозитории есть файл с тестовыми данными для их `Импорта` в приложение.
Чтобы из импортировать, зайдите в приложение, выберите раздел `Данные`, нажмите кнопку `Импорт`, и выберите файл
`fixtures.json`, который находится в корне репозитория.
После успешного импорта, новые данные будут добавлены в БД, и сразу же видны в приложении

# Решение ошибок

## Ошибка строки подключения

Представленная в примере `DB_CONNECTION_STRING` создавала БД при её отсутствии, из-за чего могут возникать ошибки вида:

```bash
CREATE DATABASE permission denied in database 'master'.
```

или 

```bash
Cannot create file "...Hotel3Db.mdf". Access is denied.
```

Это происходит, когда строка подключения пытается создать новую БД, но у пользователя нет прав.

Если вам нужно подключиться только к существующей базе, не создавая новую БД,
используйте следующую строку подключения (шаблон):

Windows Authentication (без логина/пароля)

```bash
DB_CONNECTION_STRING="Data Source=(your_server);Initial Catalog=Hotel_3;Integrated Security=True;Trust Server Certificate=True;"
```

SQL Server Authentication (с логином и паролем)
```bash
DB_CONNECTION_STRING="Data Source=(your_server);Initial Catalog=Hotel_3;User Id=your login;Password=YOUR_PASSWORD;Trust Server Certificate=True;"
```

## Ошибка миграции

При ошибке наподобии `Your startup project 'Hotel_3.WPF' doesn't reference Microsoft.EntityFrameworkCore.Design. This package is required for the Entity Framework Core Tools to work. Ensure your startup project is correct, install the package, and try again.`

В `Visual Studio` убедитесь что начальным проектом и в `Visual Studio` и в `Диспетчере пакетов NuGet` выбран `Hotel_3.EntityFramework`.

Для этого раскройте кнопку запуска проекта, выберите `Настройка начальных проектов`
, укажите `Один запускаемый проект -> Hotel_3.WPF -> ок`;

В `Диспетчере пакетов NuGet` выберите `Проект по умолчанию`: `Hotel_3.EntityFramework`

После чего, попробуйте снова

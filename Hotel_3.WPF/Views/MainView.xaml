<UserControl x:Class="Hotel_3.WPF.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Hotel_3.WPF.Views"
             xmlns:views="clr-namespace:Hotel_3.WPF.Views"
             xmlns:admin="clr-namespace:Hotel_3.WPF.Views.Admin"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920">
    
    <Grid>
        <TabControl Style="{StaticResource MaterialDesignUniformTabControl}">
            <!-- Rooms -->
            <TabItem Header="Комнаты">
                <TabControl Style="{StaticResource MaterialDesignNavigationRailTabControl}">
                    <TabItem Header="Комнаты" Content="{Binding RoomsViewModel}"/>
                    <TabItem Header="Категории" Content="{Binding CategoryViewModel}"/>
                    <TabItem Header="Статусы">
                        <admin:StatusView/>
                    </TabItem>
                </TabControl>
            </TabItem>
            <!-- Clients -->
            <TabItem Header="Клиенты">
                <!-- TODO -->
            </TabItem>
            <!-- Users -->
            <TabItem Header="Пользователи">
                <!-- TODO -->
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>

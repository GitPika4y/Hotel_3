<UserControl x:Class="Hotel_3.WPF.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920">
    
    <Grid>
        <TabControl Style="{StaticResource MaterialDesignUniformTabControl}">
            <!-- Rooms -->
            <TabItem Header="Комнаты">
                <TabControl Style="{StaticResource MaterialDesignNavigationRailTabControl}">
                    <TabItem Header="Бронирования" Content="{Binding BookingViewModel}" Width="Auto"/>
                    <TabItem Header="Комнаты" Content="{Binding RoomsViewModel}" Width="Auto"/>
                    <TabItem Header="Категории" Content="{Binding CategoryViewModel}" Width="Auto"/>
                    <TabItem Header="Статусы" Content="{Binding StatusViewModel}" Width="Auto"/>
                </TabControl>
            </TabItem>
            
            <TabItem Header="Клиенты" Content="{Binding ClientViewModel}" Width="Auto"/>
            
            <TabItem Header="Пользователи">
                <TabControl Style="{StaticResource MaterialDesignNavigationRailTabControl}">
                    <TabItem Header="Пользователи" Content="{Binding UserViewModel}" Width="Auto"/>
                    <TabItem Header="Роли" Content="{Binding RoleViewModel}" Width="Auto"/>
                </TabControl>
            </TabItem>
            
            <TabItem Header="Данные" Content="{Binding DataViewModel}" Width="Auto" />
            
        </TabControl>
    </Grid>
</UserControl>

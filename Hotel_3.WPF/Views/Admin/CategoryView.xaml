<UserControl x:Class="Hotel_3.WPF.Views.Admin.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Hotel_3.WPF.Views.Admin"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" 
            Orientation="Horizontal">
            <TextBlock Text="Категории комнат"
                       Margin="15"
                       Style="{StaticResource MaterialDesignHeadlineSmallTextBlock}"/>
            <Button Content="Добавить"
                    Command="{Binding AddCategoryCommand}"
                    Margin = "15 0"/>
            <Button Content="Обновить"
                    Command="{Binding UpdateCategoryCommand}"
                    Background="#FEB308"
                    BorderBrush="#FEB308"
                    Margin = "15 0"/>
        </StackPanel>
        
        
        <DataGrid Grid.Row="1"
            ItemsSource="{Binding Categories}"
                  Height="Auto"
                  Style="{StaticResource MaterialDesignDataGrid}"
                  IsReadOnly="True"
                  SelectedItem="{Binding SelectedItem}"/>
    </Grid>
</UserControl>

<UserControl x:Class="Hotel_3.WPF.Views.Admin.RoomsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Hotel_3.WPF.Views.Admin"
             xmlns:models="clr-namespace:Hotel_3.Domain.Models;assembly=Hotel_3.Domain"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500"
             Loaded="RoomsView_OnLoaded">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0"  
            Orientation="Horizontal">
            <TextBlock Text="Комнаты"
                       Margin="15"
                       Style="{StaticResource MaterialDesignHeadlineSmallTextBlock}"/>
            <Button Content="Добавить"
                    Command="{Binding AddRoomCommand}"/>
        </StackPanel>
        
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Rooms}"
                  Height="Auto"
                  Style="{StaticResource MaterialDesignDataGrid}"
                  IsReadOnly="True"/>
    </Grid>
</UserControl>